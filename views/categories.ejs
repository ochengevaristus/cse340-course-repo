<%- include('partials/header') %>

<h1><%= title %></h1>
<ul>
  <% categories.forEach(category => { %>
    <li><%= category %></li>
  <% }) %>
</ul>

<%- include('partials/footer') %>

<h1><%= title %></h1>

<ul>
  <% categories.forEach(category => { %>
    <li><%= category.name %></li>
  <% }) %>
</ul>